[project]
name = "bushido_client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Noe Javet", email = "np.javet@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
bushido_client = "bushido_client:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
